# Final Audit Packet

Read the following inputs:

- Agent definition: `{agent_definition_path}`
- Workflow rules: `{workflow_spec_path}`
- Memory protocol: `{memory_protocol_path}`
- Curated memory: `{memory_path}`
- Topic memory index: `{memory_topics_path}`
- Session log: `{session_log_path}`
- Decision index: `{decisions_index_path}`
- Latest accepted draft: `{draft_path}`
- Review summary that cleared the run: `{review_summary_path}`
- LaTeX root: `{latex_root_path}`

Task:

- Perform a final editorial pass for coherence, notation, citations, and portability into the LaTeX manuscript.
- Keep the manuscript within the already approved substantive boundaries.
- Record final edits and any remaining layout work for `paper/sections/*.tex`.
- Use Layer 4 only if you need exact historical wording or command details that are not already summarized in Layers 1 to 3.
