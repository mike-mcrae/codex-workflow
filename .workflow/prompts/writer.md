# Writing Packet

Read the following inputs:

- Agent definition: `{agent_definition_path}`
- Workflow rules: `{workflow_spec_path}`
- Memory protocol: `{memory_protocol_path}`
- Curated memory: `{memory_path}`
- Topic memory index: `{memory_topics_path}`
- Session log: `{session_log_path}`
- Decision index: `{decisions_index_path}`
- Approved plan: `{plan_path}`
- Project brief: `{project_brief_path}`
- Source notes: `{source_notes_path}`
- LaTeX root for eventual promotion: `{latex_root_path}`

Task:

- Draft the manuscript at `{output_path}` using `{draft_template_path}`.
- Follow the approved plan rather than re-planning.
- Make claims proportional to evidence.
- Preserve economics-paper discipline: identification, contribution, literature, limitations, and precise interpretation.
- Leave explicit placeholders for evidence or citations that do not yet exist.
- Treat the memory layers explicitly: read Layer 1 and the latest 3 to 5 Layer 3 entries now, cite Layer 2 only if you are reusing a settled choice, and keep Layer 4 out of normal context unless you need an exact prior command, error, or wording.
