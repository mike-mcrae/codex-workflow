# Planning Packet

Read the following before writing anything:

- Agent definition: `{agent_definition_path}`
- Workflow rules: `{workflow_spec_path}`
- Memory protocol: `{memory_protocol_path}`
- Project config: `{project_config_path}`
- Quality gates: `{quality_gates_path}`
- Curated memory: `{memory_path}`
- Topic memory index: `{memory_topics_path}`
- Session log: `{session_log_path}`
- Decision index: `{decisions_index_path}`
- Project brief: `{project_brief_path}`
- Source notes: `{source_notes_path}`

Task:

- Produce the full writing plan at `{output_path}`.
- Use `{plan_template_path}` as the output structure.
- Define the argument, contribution, empirical strategy, evidence requirements, section plan, and writing risks.
- Keep the work at the planning level only.
- Set `status: approved` only if the plan is truly ready for drafting.
- Use the memory layers correctly: read Layer 1 and the latest 3 to 5 Layer 3 entries now; Layer 2 is cited only if an old choice needs justification; Layer 4 is retrieval-only unless exact prior detail is needed.
