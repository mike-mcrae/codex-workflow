# Scripts

Use this directory for researcher-authored project code.

- `python/`: Python scripts and modules
- `stata/`: Stata do-files
- `r/`: R scripts
- `shell/`: shell helpers tied to the research project

Internal workflow tooling lives in `.workflow/scripts/`, not here.
